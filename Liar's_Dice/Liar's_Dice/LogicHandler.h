#pragma once
#include <vector>

using std::vector;
using std::string;

bool raise(const vector<int>*, const vector<int>*);
bool callBluff(vector<int>*, vector<int>*);
bool spotOn(vector<int>*, vector<int>*);


bool containsElements(vector<int>, vector<int>);
bool vecContains(vector<int>*, int);
bool sameValues(const vector<int>*);
bool validDice(const vector<int>*);
bool validDie(int);
